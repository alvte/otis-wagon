<% if @chatroom.from_marketplace? || @chatroom.from_card_marketplace? %>
  <%= render partial: "shared/chat_marketplace", locals: {chatroom: @chatroom} %>
<% else %>
  <%= render partial: "shared/chat_card", locals: {chatroom: @chatroom} %>
<% end %>
